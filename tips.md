继续优化已经落到主进程视频解码链路，重点改在 main/stream/session.ts。这次我参
考了 chiaki-ng 的两个点来修正方向：

- ffmpegdecoder.c 的核心思路是不要在压缩流和解码输出的中间随意丢字节，而是让解
码器自己消化旧帧，再只取最新帧显示。
- videoreceiver.c 的核心思路是出错后等待干净的同步帧恢复，不让解码器长期带着坏
参考帧继续跑。